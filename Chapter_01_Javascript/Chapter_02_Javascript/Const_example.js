const BaseUrl ="https://api.example.com/data";
console.log(BaseUrl);

/* const cannot be redeclared or reassigned.
 It is block-scoped and is used for values that should not change throughout the program.
 */